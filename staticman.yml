allowedFields: ['name', 'email', 'url', 'message']
branch: "master"
commitMessage: "New post comment"
filename: "comment-{@timestamp}"
format: "yaml"
moderation: true
path: "_data/comments/{options.slug}"
requiredFields: ['name', 'email', 'message']
transforms:
  email: md5
generatedFields:
  date:
    type: date
    options:
      format: "timestamp-seconds"
